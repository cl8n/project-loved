Here are the results!

<? if (vars.PASSED_BEATMAPSETS.length !== 0) {
    `The following maps reached the ${vars.THRESHOLD} threshold needed to be moved to Loved this round:\n${vars.PASSED_BEATMAPSETS}\n\n`
} ?><?
if (vars.FAILED_BEATMAPSETS.length !== 0) {
    `The following maps [b]did not[/b] reach the ${vars.THRESHOLD} threshold, so they will not be moved to Loved:\n${vars.FAILED_BEATMAPSETS}\n\n`
} ?>
[b]<3[/b]
